server:
  port: 9000

spring:
  application:
    name: service-consumer
eureka:
  client:
    register-with-eureka: false
    registry-fetch-interval-seconds: 10
    service-url:
      defaultZone:  http://eureka-server1:8888/eureka/eureka,http://eureka-server1:8889/eureka/eureka


